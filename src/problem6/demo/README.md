# Scoreboard Demo

Minimal runnable demo for the Problem 6 specification.

## Run

```bash
npm install
npm start
```

Server: http://localhost:3000  
Client: open `client.html` in a browser

## Test users

All seeded players use password `pass123`. Usernames: Minh, Lan, Huy, Trang, Duc, Mai, Khoa, Linh, Nam, Anh.

## Tests

```bash
npm test
```

Tests import the Express app directly (no running server required).
